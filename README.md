# errors-publisher
### How to publish a new version.
1. Make you changes and commit them
2. Run `sbt publish`
3. `cd ../errors-publisher-repository`
4. `git add .`
5. `git commit -m "Release version <VERSION>"`
6. `git push origin repository`